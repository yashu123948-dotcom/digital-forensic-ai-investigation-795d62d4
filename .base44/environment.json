{
  "previewPort": 3000,
  "healthPath": "/",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "SUPABASE_SERVICE_ROLE_KEY",
      "description": "Supabase service role key (server-only, bypasses RLS). Needed for admin/server-side operations. The public publishable key in .env is enough to boot and browse; provide this only to enable admin features.",
      "requiredAtBoot": false,
      "generatedForDevelopment": false
    }
  ]
}
